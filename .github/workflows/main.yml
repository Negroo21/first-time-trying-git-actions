on:
  push:
    branches:
       - master





jobs:
  build:
    runs-on: ubuntu-latest  

    steps:
      - uses: actions/checkout@v3

      - name: "Login to Docker hub"
        run: docker login --username mostafanasser123 --password dckr_pat_2bQ9akff9aiEZb23ukBZewgcnus